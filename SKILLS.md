# Skills

> Generated by `tools/spine.mjs` from live `SKILL.md` metadata and `skills/skills-help/taxonomy.yon`. Do not edit this catalog by hand.

This pack contains **57 skills**. Install only what earns its place; every skill is readable Markdown, and 39 also carry a declarative YON (YounndAI Object Notation™) protocol you can inspect and validate.

Required companions are bundled inside their skill folder. Optional companions are repository checks or sibling enhancements and are declared separately in the machine catalog.

A skill folder name is its portable written command. The trigger column shows recognition aliases in plain text, not additional canonical commands.

## Planning & execution

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`plan-cleanup`](skills/plan-cleanup/) | Post-execution cleanup — remove planning artifacts and tech debt left behind after a plan runs. | /plan-cleanup, clean up artifacts, remove the plan file | Markdown + YON |
| [`plan-create`](skills/plan-create/) | Propose a phased, gated implementation plan before any work begins. | /plan-create, make a plan, plan this out | Markdown + YON |
| [`plan-deep-dive`](skills/plan-deep-dive/) | Phase-by-phase deep inspection of any multi-phase plan — stop at each phase and assess completeness, quality, and future-readiness before advancing. | /deep-dive, do a deep dive, inspect each phase | Markdown + YON |
| [`plan-evolve`](skills/plan-evolve/) | Evolve a target to its next version — treat what shipped as v1, plan the next iteration. | what would v2 look like, evolve this, what's next for this | Markdown |
| [`plan-execute`](skills/plan-execute/) | Apply the last approved plan exactly. | /execute, execute the plan, implement this | Markdown + YON |
| [`plan-phases`](skills/plan-phases/) | Restructure an existing plan into phases with /verify gates after each phase and critical steps. | /phase-plan, add phases to this plan, gate this plan | Markdown + YON |
| [`reflect`](skills/reflect/) | Lightweight cognitive pause — surface assumptions, uncertainty, and what you'd do differently. | /reflect, step back and think before continuing | Markdown |
| [`verify`](skills/verify/) | Formal phase gate — checks intent, plan, and execution artifacts for drift, then actively re-checks each load-bearing claim against its source (an agent's self-report of having checked is not evidence). | /verify | Markdown + YON |

## Insight & decision

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`cold-review`](skills/cold-review/) | Run outside-agent review of actual work artifacts against objectives, with classification, fresh reviewer lenses, evidence-based findings (re-opened at synthesis, not recalled), scoring, and thresholds; the review self-verifies its own findings before returning. | /cold-review, cold review this, get outside review | Markdown + YON |
| [`double-check`](skills/double-check/) | Targeted re-verification of a specific claim, output, or decision. | /double-check, verify this, re-check this | Markdown + YON |
| [`insight-adversarial`](skills/insight-adversarial/) | Multi-pass adversarial critique from dynamically selected personas — including Claude's own POV. | /insight-adversarial, adversarial, red team this | Markdown + YON |
| [`insight-angles`](skills/insight-angles/) | Angle & connection discovery engine. | /insight-angles, find unseen angles, what connections am I missing | Markdown + YON |
| [`insight-assess`](skills/insight-assess/) | Structured decision evaluation — pros/cons, impact assessment, quality analysis, recommendation. | /assess, /insight-assess | Markdown + YON |
| [`insight-critique`](skills/insight-critique/) | Advisory review workflow. | /critique, review this, critique this | Markdown + YON |
| [`insight-cross-examine`](skills/insight-cross-examine/) | Context-routed deliberation engine. | /insight-cross-examine, cross-examine this, weigh these options | Markdown + YON |
| [`insight-explore`](skills/insight-explore/) | Brainstorm 3+ possible implementations before committing. | /explore, brainstorm options, what are the ways to do this | Markdown + YON |
| [`insight-retro`](skills/insight-retro/) | Post-implementation retrospective — map blast radius, audit documentation, extract skills, update KIs, document lessons learned. | /retro, retrospective, post-mortem | Markdown + YON |
| [`insight-skill-gap`](skills/insight-skill-gap/) | Scan recent work and identify if new skills should be created or existing ones updated. | /skill-gap, check for skill gaps, what skills should we add | Markdown + YON |
| [`investigate`](skills/investigate/) | Gather facts (files, deps, patterns) without making changes. | /investigate, look into this, explore the codebase | Markdown + YON |

## Priming

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`extract-signal`](skills/extract-signal/) | An elastic signal-extraction methodology run under /multi-agent-mode. | /extract-signal, extract the signal on X and present it as Y, gather + vet + report the signal about X | Markdown + YON |
| [`prime-expand`](skills/prime-expand/) | Expand a vague gap into concrete questions + named source candidates via a delegated sub-agent — caller sees only the proposed pair for confirm/edit/refuse, never the expansion reasoning. | /prime-expand, expand on this | Markdown + YON |
| [`prime-fetch`](skills/prime-fetch/) | Run a noisy data-fetching tool call (Grep, Glob, WebFetch, MCP search) on the caller's behalf in a sub-agent's context — only the filtered, proof-of-work-verified digest enters the caller's window. | /prime-fetch | Markdown + YON |
| [`prime-sweep`](skills/prime-sweep/) | Prime the calling agent with high-signal context from large source surfaces (multiple files, packages, folders) via parallel delegated sub-agents. | /prime-sweep | Markdown + YON |

## Skill library

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`new-skill-creator`](skills/new-skill-creator/) | Scaffold a new Agent Skill end-to-end into a skills repo you maintain (folder, SKILL.md, optional protocol.yon for dual-doc skills, tri-runtime links for Claude Code + Codex + the shared .agents/ dir, commit and push). | /new-skill-creator, create a skill, make a new skill for X | Markdown + YON |
| [`next-skills`](skills/next-skills/) | The Next Skills protocol (NSP). | /next-skills | Markdown + YON |
| [`self-improve`](skills/self-improve/) | The Self-Improvement Protocol (SIP). | /self-improve, /self-improve [skill] | Markdown + YON |
| [`skills-help`](skills/skills-help/) | The skills library menu — every skill grouped by family, each with a one-line description and a dual-doc/single-doc marker; pass a family name to drill into one family in full detail. | /skills-help, /skills-help {family}, list my skills | Markdown |

## Orchestration & dispatch

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`agent-mailbox`](skills/agent-mailbox/) | Two or more agents — any harness, any vendor — collaborate through any shared folder: share a folder, load the skill, point at it. | /agent-mailbox, handshake with the other agent, collaborate through this inbox | Markdown + YON |
| [`ask-gate`](skills/ask-gate/) | The Ask-Gate Protocol (AGP). | /ask-gate | Markdown + YON |
| [`budget-check`](skills/budget-check/) | Pre-wave usage gate. | /budget-check | Markdown + YON |
| [`caller-options`](skills/caller-options/) | The Caller Options protocol (COP). | /caller-options | Markdown + YON |
| [`handoff`](skills/handoff/) | Generate a structured cold-agent handoff brief (YAML frontmatter + Obsidian markdown + YON blocks) that any fresh LLM session can pick up and execute. | /handoff, create a handoff, dispatch to a fresh agent | Markdown + YON |
| [`handoff-execute`](skills/handoff-execute/) | Execute a handoff brief with enforced source-read discipline — load the brief's @SOURCES.required tier fully before planning, cite sources per plan step, run @OUTCOME.verify after, emit the retro per @GROUND_TRUTH_REQUIRED. | /handoff-execute, /handoff-execute <brief-path> | Markdown + YON |
| [`multi-agent-mode`](skills/multi-agent-mode/) | Activate multi-agent mode: the lead agent still works directly but delegates independent helper slices when useful. | /multi-agent-mode, multi-agent mode, use helper agents | Markdown |
| [`orchestrate-mode`](skills/orchestrate-mode/) | Activate orchestrator mode in the current session: the lead agent dispatches governed worker subagents and does not execute concrete work directly. | /orchestrate-mode, become an orchestrator, orchestrator mode | Markdown |

## Orientation

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`orient-gaps`](skills/orient-gaps/) | Answers "What is stuck or missing?" on any subject — blockers (on you vs on others), open forks, loose ends, and inferred silent gaps, computed fresh via a bounded read-only subagent. | /orient-gaps, what's stuck, what's not done | Markdown + YON |
| [`orient-map`](skills/orient-map/) | Answers "What changed since I last looked?" on any subject — the delta since your last look, plus done → here → next as a path on trunk or a TREE on a branch, computed fresh via a bounded read-only subagent. | /orient-map, show me the map, what changed since last look | Markdown + YON |
| [`orient-roadmap`](skills/orient-roadmap/) | Answers "What comes next, and what gates it?" on any subject — the increment arc (built → current → next), what shipped now, the gates, the next increment's clusters (paramount starred), and the runway of stage boxes (now → gated-next → future) plus the deferred lanes, computed fresh via a bounded read-only subagent. | /orient-roadmap, show me the roadmap, show me the runway | Markdown + YON |
| [`orient-status`](skills/orient-status/) | Answers "How far along am I, and when will it be done?" on any subject (repo, folder, plan, task) — current position, what's left, and a banded ETA, computed fresh via a bounded read-only subagent. | /orient-status, where are we on this, status quo and eta | Markdown + YON |

## Human output

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`human-draw`](skills/human-draw/) | Renders information as a picture a person reads at a glance. | /human-draw, draw this, draw it for me | Markdown |
| [`human-merge`](skills/human-merge/) | Takes several separate reports — from several agents, several sources, or several documents — and returns one surface a person can decide from. | /human-merge, merge these reports, combine what the agents found | Markdown |
| [`human-output`](skills/human-output/) | The contract for writing anything a person will read and decide from — a report, a finding, an answer, a request for a decision. | /human-output, remember I am a person not a machine, write this for a human | Markdown |
| [`human-rewrite`](skills/human-rewrite/) | The repair pass on text that already exists — an agent's own last output, a subagent's report, a supplied document, a research summary, a legal or medical paragraph — rewritten so a person can read it fast and act on it, without changing what it claims. | /human-rewrite, translate that into human, that was a wall of text | Markdown |

## Code & architecture

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`audit-coupled-constant`](skills/audit-coupled-constant/) | Audit every site of a constant whose value is duplicated across multiple call sites (retry budgets, timeouts, page sizes, magic numbers, feature flags). | /audit-coupled-constant, audit budget consistency, find all sites of this value | Markdown |
| [`design-an-interface`](skills/design-an-interface/) | Generate multiple radically different interface designs for a module, then compare and synthesize. | /design-an-interface, design an API, explore interface options | Markdown |
| [`improve-codebase-architecture`](skills/improve-codebase-architecture/) | Explore a codebase to find opportunities for architectural improvement by deepening shallow modules (John Ousterhout, "A Philosophy of Software Design"). | /improve-codebase-architecture, improve architecture, find refactoring opportunities | Markdown + YON |
| [`monorepo-deps`](skills/monorepo-deps/) | End-to-end syncpack + manypkg flow for an npm monorepo: check, fix, update, and maintain `.syncpackrc.json`. | /monorepo-deps, update deps, sync deps | Markdown + YON |

## Obsidian & vault

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`json-canvas`](skills/json-canvas/) | Create and edit JSON Canvas files (.canvas) with nodes, edges, groups, and connections. | make a canvas, build a mind map, draw a flowchart in Obsidian | Markdown |
| [`obsidian-bases`](skills/obsidian-bases/) | Create and edit Obsidian Bases (.base YAML files) with views, filters, formulas, and summaries. | make a base, build a database view of my notes, add a filter/formula to this base | Markdown |
| [`obsidian-cli`](skills/obsidian-cli/) | Drive a running Obsidian instance via the `obsidian` CLI — search vault, open/create/move notes, toggle tasks, edit properties, reload plugins, run JS, capture errors/screenshots. | search my vault, open this note in Obsidian, list notes tagged X | Markdown |
| [`obsidian-markdown`](skills/obsidian-markdown/) | Write valid Obsidian-Flavored Markdown — wikilinks, embeds, callouts, frontmatter properties, tags, comments. | write this as an Obsidian note, add a callout, use wikilinks | Markdown |

## YON

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`yon-read`](skills/yon-read/) | Read, interpret, or explain existing YON content. | read this YON, what does this YON mean, explain this @DOC/@RULE/@STEP | Markdown |
| [`yon-write`](skills/yon-write/) | YON Writing & Compilation Mode. | write YON, convert to YON, create a .yon file | Markdown |

## Web

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`defuddle`](skills/defuddle/) | Extract clean markdown from web pages via Defuddle CLI (strips nav/ads/clutter, saves tokens). | read this article, fetch this page, summarize this blog post | Markdown |
| [`domain-check`](skills/domain-check/) | Reconcile domain-name availability across sources that disagree. | /domain-check, is this domain available, check these domain names | Markdown + YON |

## Git & sync

| Skill | Use when | Example triggers | Format |
|---|---|---|---|
| [`diff-recap`](skills/diff-recap/) | Turn a git diff into a PR-pasteable recap — one row per changed file whose path, status, and line counts are transcribed verbatim from `git diff --numstat` (true by construction; the model writes only the labels), emitted as an inline annotated widget plus a mandatory ASCII twin and a `diff-recap/1` record whose totals equal the sum of the rows. | /diff-recap, recap this diff, summarize these changes for a PR | Markdown + YON |
| [`github-sync`](skills/github-sync/) | Review the session's git changes, summarize them for approval, then commit and push to GitHub. | /github-sync, sync to github, commit and push | Markdown + YON |

---

open-skills is a personal project by Alexandru Mares (allemaar.com), separate from the YounndAI™ product portfolio. Apache-2.0. YON (YounndAI Object Notation™), Lyt (Link Your Think™), and YounndAI™ are trademarks of MARLINK TRADING SRL.
