# Fixture — a human- family contract whose roster silently omits a shipped member

This is the `human-merge` case verbatim: the member shipped while the routing
table still said "not yet shipped", and no tool read the table, so nothing
noticed. Every name below is real; the defect is the ONE that is absent.

## 1. The roster and the routing table

| The material you are holding | Member | Why |
|---|---|---|
| One fact, one finding, one verdict | `human-output` | Real, shipped, listed. |
| Relational, positional, proportional | `human-draw` | Real, shipped, listed. |
| Text that already exists but is unreadable | `human-rewrite` | Real, shipped, listed. |

## 2. The stopping-point doctrine

(end of fixture — `human-merge` ships but is named nowhere above.)
