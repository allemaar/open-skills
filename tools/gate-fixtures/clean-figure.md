**Storage is dominated by the archive tier; the rest is noise.**

Every bar below is drawn at one cell per hundred units, so the picture and the
printed numbers agree. The fence holds printable ASCII only and stays inside the
column budget.

```
archive  ##############  1400
logs     #####            500
cache    #                100
```
