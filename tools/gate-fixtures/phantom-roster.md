# Fixture — a human- family contract whose roster names a member that does not ship

## 1. The roster and the routing table

| The material you are holding | Member | Why |
|---|---|---|
| One fact, one finding, one verdict | `human-output` | Real, shipped. |
| Relational, positional, proportional | `human-draw` | Real, shipped. |
| Text that already exists but is unreadable | `human-rewrite` | Real, shipped. |
| Several separate reports that must become one | `human-merge` | Real, shipped. |
| Something nobody ever built | `human-ghost` | THE DEFECT — no `skills/human-ghost/` directory exists. |

## 2. The stopping-point doctrine

(end of fixture)
