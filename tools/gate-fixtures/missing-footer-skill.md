# Fixture — a skill that cites the human-output contract but drops its footer

The body below opts into the contract by naming `human-output/SKILL.md`, so the
footer is mandatory. What follows the citation is a PARAPHRASE, not the verbatim
blockquote — which is exactly the drift a growing family produces.

## Reporting

Report findings per the human-output contract (`human-output/SKILL.md`): verdict
first, confidence labelled, and say what you did not check.

> **Human output.** Output from this skill follows the human-output contract.

(end of fixture)
