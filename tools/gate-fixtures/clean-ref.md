# Fixture — a companion doc whose references all resolve

This file cites `tools/lint.mjs` and `README.md` — both real, both shipped. The
positive control: the reference check must accept it, so a green run means "checked
and clean", not "the check never looked".
