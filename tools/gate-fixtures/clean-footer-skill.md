# Fixture — a skill that cites the human-output contract and carries its footer

Identical in shape to `missing-footer-skill.md`, but the last block is the
verbatim two-line blockquote. A green run here means the check read the file and
found the footer, not that it never looked.

## Reporting

Report findings per the human-output contract (`human-output/SKILL.md`): verdict
first, confidence labelled, and say what you did not check.

> **Human output.** This skill's handler-facing output obeys the human-output
> contract (`human-output/SKILL.md`).
