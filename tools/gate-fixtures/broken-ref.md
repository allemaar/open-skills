# Fixture — a companion doc with a broken reference

This file cites `docs/nsp-cop-audit.md`, a path that does not exist in the public
pack — the exact class (a private-repo leftover kept on export) that the widened
`tools/lint.mjs` was taught to see. The reference check must reject this file.
