# Fixture — a skills-help menu that lists a skill the pack does not ship

## The menu

### Some family
- ◆ `verify` — a real, shipped skill (must not trip the guard)
- ◆ `ghost-skill` — a skill with no `skills/ghost-skill/` directory

## Family drill-down

(end of fixture)
